<div class="container">
  <h1> Hello from new test </h1>

  <%= simple_form_for(@test) do |f| %>
    <%= f.input :title %>
    <%= f.input :category %>
    <%= f.input :user_id, as: :hidden, input_html: { value: current_user } %>
    <%= f.submit %>
  <% end %>
</div>
